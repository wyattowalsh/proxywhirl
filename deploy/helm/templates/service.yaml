apiVersion: v1
kind: Service
metadata:
  name: {{ include "proxywhirl.fullname" . }}
  labels:
    {{- include "proxywhirl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proxywhirl.selectorLabels" . | nindent 4 }}
