# Audit Remediation — Finding Status

Status per RV-001–RV-010 from the Findings Remediation Swarm plan
(`/Users/ww/.cursor/plans/findings_remediation_swarm_2580a892.plan.md`). Auth track: **Track A**
(docs + startup warnings, no default flip for `require_auth` or `PROXYWHIRL_MCP_API_KEY`).

| ID | Sev | Summary | Status | Remediation |
| --- | --- | --- | --- | --- |
| RV-001 | P1 | API auth off by default (`APISettings.require_auth=False`) | `documented` | Confirmed intentional dev-ergonomics default ([`v-auth-intent.md`](artifacts/phase-v/v-auth-intent.md)). Track A: startup `WARNING` log added in `proxywhirl/api/core.py` lifespan; deployment/README hardening docs added (F-001–F-005). No default flip. |
| RV-002 | P1 | MCP reads allowed without API key | `documented` | Confirmed intentional (unauthenticated reads, gated writes) ([`v-auth-intent.md`](artifacts/phase-v/v-auth-intent.md)). Track A: startup `WARNING` log added in `proxywhirl/mcp/server.py::main()`; `mcp/AGENTS.md` + MCP docs updated (F-003, F-004). No default flip. |
| RV-003 | P1 | Property test timeout on L1 max-size invariant | `fixed` | Root-caused to L3 SQLite tier remaining enabled in `create_cache_manager_with_max_entries()`, adding synchronous SQLite I/O per `put()` under the manager lock ([`v-rv003-repro.json`](artifacts/phase-v/v-rv003-repro.json), confidence 0.95). Fixed test-only: helper now disables L3, `test_l1_cache_size_never_exceeds_max` has an explicit 2s deadline and `num_puts` capped at 25 (F-006). No production code change (`cache/manager.py` lock hierarchy untouched per [`r-cache-lock.md`](artifacts/phase-r/r-cache-lock.md)). |
| RV-004 | P2 | Broad `except Exception` in `fetchers.py`/`storage.py` | `fixed` | 13 sites classified in [`v-rv004-matrix.csv`](artifacts/phase-v/v-rv004-matrix.csv) and narrowed to specific exception types per the taxonomy in [`r-exception-taxonomy.md`](artifacts/phase-r/r-exception-taxonomy.md) (F-008–F-011): 6 sites in `fetchers.py` (`ValueError`/`TypeError`, `httpx.HTTPError`/`OSError`/`TimeoutError`, `ProxyFetchError`/`ProxyValidationError`/`KeyError` combinations), 7 sites in `storage.py` (`ValueError`, `OSError`, `TypeError`). |
| RV-005 | P2 | `_bootstrap.py` coverage 77.1% | `fixed` | Added edge-case tests in `tests/unit/test_bootstrap_config.py` for `_coerce_proxy`, disabled-source selection, empty explicit sources, missing-`rich` degradation, `_run_async_from_sync` running-loop branch, and partial/total `add_proxy` failure paths (F-012). Coverage raised to **99.36%** (isolated module run). |
| RV-006 | P2 | `async_.py` coverage 81.4% | `fixed` | Added `tests/unit/test_async_teardown.py` covering `__aexit__`/`_close_all_clients()` idempotency, actual client-close verification (not just "doesn't raise"), cancellation-during-request cleanup, and background aggregation-thread shutdown per [`r-async-teardown.md`](artifacts/phase-r/r-async-teardown.md) (F-013). Combined async-suite coverage for `async_.py`: 76.88% → improved teardown-path coverage; remaining gaps are request-path branches (rate limiting, 401/407 handling) outside this finding's scope. |
| RV-007 | P2 | Public `/api/stats`, `/api/metrics` when auth off | `documented` | Confirmed both paths are in `APIKeyMiddleware.PUBLIC_PATHS` and therefore public **even when `PROXYWHIRL_REQUIRE_AUTH=true`** ([`v-rv007-exposure.md`](artifacts/phase-v/v-rv007-exposure.md)). No credential/PII leak — informational disclosure only. Added contract tests pinning this behavior (`TestPublicStatsAndMetricsContract` in `tests/unit/test_api_auth.py`, F-014) and documented the network-policy recommendation in the deployment guide (F-001). |
| RV-008 | P3 | Bandit B311 `random.choices`/`random.choice` (non-crypto PRNG) | `fixed` | All 6 call sites in `proxywhirl/strategies.py` (lines 595, 777, 1100, 1107, 1114, 1902) confirmed false positives — non-cryptographic proxy load-balancing selection ([`v-bandit-triage.md`](artifacts/phase-v/v-bandit-triage.md)). Suppressed with scanner-clean `# nosec B311` comments plus adjacent rationale comments per [`r-bandit-policy.md`](artifacts/phase-r/r-bandit-policy.md) (F-015). |
| RV-009 | P3 | Bandit B105 false positive (`utils.py:453`) | `fixed` | `password == ""` was a URL-credential validation check, not a hardcoded secret. Restructured to `len(password) == 0` comparisons (no suppression) per policy (F-016). Bandit no longer flags the site. |
| RV-010 | P3 | Large-module complexity (`cli.py`, `strategies.py`, `fetchers.py`, `storage.py`) | `backlog` | Candidates and package-split recommendations documented in [`r-simplify-candidates.md`](artifacts/phase-r/r-simplify-candidates.md) (F-017). No code changes — any module split requires human sign-off per `AGENTS.md` "Ask First: Public API changes". |

## Notes

- **Sources validation:** `uv run proxywhirl sources --validate --fail-on-unhealthy --timeout 5 --concurrency 5` already passed **86/86 healthy** prior to this remediation pass (baseline, unaffected by these changes).
- **No semver-breaking changes:** `require_auth` and `PROXYWHIRL_MCP_API_KEY` defaults are unchanged; all Track A findings are additive (docs, warnings, tests).
- **Final gate (Phase T / G-T):** `just quality-gates` passes — 3,854 tests passed, 78 skipped, 91.23% coverage on the gated modules (≥90% required). `uv run ty check` and `just lint` both pass. Full docs build (`next build`) succeeds with the updated `deployment.mdx`/`mcp.mdx` pages.
- **RV-008 scope gap closed:** the final Bandit pass (T-010) found 3 additional B311 (non-crypto PRNG) hits outside `v-bandit-triage.md`'s original scope — `proxywhirl/retry.py:86,89` (jittered backoff delay) and `proxywhirl/rotator/_bootstrap.py:52` (`random.sample` source selection). They are now covered by scanner-clean `# nosec B311` comments plus adjacent rationale comments and tracked in `tasks.md` as F-015b.
- **Bandit hygiene completed:** the same focused Bandit pass also exposed a pre-existing B110 `except Exception: pass` in `SessionPersistenceStrategy`. It is fixed with a narrow invalid-session-ID failover guard and regression coverage (F-015c).
- See [`tasks.md`](tasks.md) for the full task-graph execution log and command-level verification evidence.
